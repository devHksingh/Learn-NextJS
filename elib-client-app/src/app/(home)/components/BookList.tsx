import { Book } from '@/types'
import React from 'react'

const BookList = ({books}:{books:Book[]}) => {
  return (
    <div>BookList</div>
  )
}

export default BookList

// data fetching
// server components
// client component